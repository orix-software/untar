;----------------------------------------------------------------------
;
;----------------------------------------------------------------------
.delmacro mkdir
XGETCWD = $48

;----------------------------------------------------------------------
;
;----------------------------------------------------------------------
.macro	crlf
	.byte $00, XCRLF
.endmacro

;----------------------------------------------------------------------
;
;----------------------------------------------------------------------
.macro unlink fname
	; sortie: A=0 -> OK, ENOENT -> fichier inexistant
	lda	#<fname
	ldy	#>fname
	.byte $00, XRM
.endmacro

;----------------------------------------------------------------------
;
;----------------------------------------------------------------------
;.macro exists fname
;	.local true
;
;		fopen	fname, O_RDONLY
;		cmp	#$ff
;		bne	true
;		cpx	#$ff
;		bne	true
;
;	; false:
;		lda	#$ff
;		rts
;
;	true:
;		.byte $00, XCLOSE
;		lda	#$00
;		rts
;.endmacro

;----------------------------------------------------------------------
;
;----------------------------------------------------------------------
;.macro getcwd dest
;	.local loop
;		.byte $00, XGETCWD
;		sta	ptr
;		sty	ptr+1
;		ldy	#$ff
;	loop:
;		iny
;		lda	(ptr),y
;		sta	dest,y
;		bne	loop
;.endmacro


